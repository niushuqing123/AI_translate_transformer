# multi30k_en_de Placeholder

This directory is generated by `python scripts/prepare_data.py`.

The tracked repository keeps only this placeholder so the folder layout is clear.

Locally generated files will include:

- `train.src`, `train.tgt`
- `valid.src`, `valid.tgt`
- `test.src`, `test.tgt`
- `src_vocab.json`, `tgt_vocab.json`
- `meta.json`
