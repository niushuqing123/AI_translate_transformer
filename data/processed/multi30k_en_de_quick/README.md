# multi30k_en_de_quick Placeholder

This directory stores the quick-run processed variant used for smoke tests.

It is generated locally and not versioned with its data contents. Recreate it with:

```bash
python scripts/prepare_data.py
```
