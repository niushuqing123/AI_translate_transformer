# Multi30k Raw Placeholder

This folder is kept as a placeholder only.

The actual `.gz` files are not stored in git. Recreate them with:

```bash
python scripts/download_multi30k.py
```

After download, this directory will contain the raw English and German splits plus `download_manifest.json` locally.
